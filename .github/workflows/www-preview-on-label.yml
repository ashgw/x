name: WWW Preview on Label

on:
  pull_request:
    types:
      - labeled

permissions:
  pull-requests: write
  contents: read
  issues: write

jobs:
  trigger-preview:
    if: github.event.label.name == 'ready-for-preview'
    uses: ./.github/workflows/www-preview-deployment.yml
    secrets: inherit
